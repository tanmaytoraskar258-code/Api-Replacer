# Baseline Asynchronous Network Engine Extensions
asyncio>=3.4.3
typing-extensions>=4.8.0
# Optional Extensions for Production Multi-Node API Replacements
uvloop>=0.19.0; sys_platform != 'win32' # Hyper-fast event loops for Unix hardware
